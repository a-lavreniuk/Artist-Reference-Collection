# Руководство по модальным окнам в ARC

## 🎨 Типы модальных окон

### 1. Modal (.modal--medium) — Создание/редактирование
- **Размер:** 822px
- **Позиция:** центр экрана
- **Стиль:** padding 40px, gap 16px, без крестика
- **Использование:** Создание коллекций, категорий, меток

### 2. Dialog (.dialog) — Системные действия
- **Размер:** 822px
- **Позиция:** центр экрана
- **Стиль:** padding 40px, кнопки S справа, текст слева, без иконки
- **Использование:** Изменение хранилища, восстановление резервной копии

### 3. Toast — Подтверждение удалений
- **Размер:** 480px
- **Позиция:** правый нижний угол
- **Стиль:** padding 24px, заголовок + описание + кнопки S
- **Особенность:** Всегда в одном экземпляре
- **Использование:** Все удаления, очистка, отмена действий

### 4. Alert Banner — Информирование
- **Размер:** авто (min 400px, max 600px)
- **Позиция:** внизу по центру
- **Стиль:** padding 16px, скругления 16px, иконки 16px
- **Автозакрытие:** 5 секунд
- **Использование:** Успехи, ошибки, предупреждения, процессы

---

## ✅ Реализованные действия

### Toast (удаления и отмены)
- Удаление карточки
- Удаление коллекции
- Удаление категории
- Очистка мудборда
- Очистка истории
- Отмена добавления карточек
- Неполная очередь файлов

### Alert Banner (информирование)
- Коллекция/категория/метка создана
- Карточка/коллекция/категория удалена
- Карточки добавлены
- ID карточки скопирован
- Настройки скопированы/применены
- Файл экспортирован
- Все ошибки и предупреждения

---

## 📝 Использование

### Показать Toast с подтверждением
```typescript
const toast = useToast();

toast.showToast({
  title: 'Удалить карточку',
  message: 'Вы уверены что хотите удалить карточку? Это действие необратимо',
  type: 'error',
  onConfirm: async () => {
    // Код удаления
  },
  confirmText: 'Удалить',
  cancelText: 'Отмена'
});
```

### Показать Alert Banner
```typescript
const alert = useAlert();

alert.success('Коллекция создана');
alert.error('Не удалось сохранить');
alert.warning('Превышен лимит файлов');
alert.info('ID карточки скопирован');
```

### Быстрые методы Toast
```typescript
toast.success('Сообщение'); // Простое уведомление (автозакрытие)
toast.error('Ошибка');
toast.info('Информация');
```

---

## 🔑 Ключевые особенности

1. **Toast всегда один** — новый заменяет предыдущий
2. **Alert автозакрывается** — через 5 секунд
3. **Toast с onConfirm** — не закрывается автоматически
4. **Все стили inline** — гарантированное применение

