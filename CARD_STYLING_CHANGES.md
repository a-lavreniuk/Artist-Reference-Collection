# Изменения в стилизации карточек

## Дата: 9 ноября 2025
## Ветка: `feature/card-styling`

---

## ✅ Выполненные изменения

### 1. **Скругление углов карточки**
- Установлено значение `border-radius: 16px` (вместо переменной `--radius-m` = 12px)
- Файл: `renderer/src/components/common/Card.css`

### 2. **Фон контентной области**
- Изменен цвет фона `.layout__content` на белый (`background-color: white`)
- Файл: `renderer/src/components/layout/Layout.css`

### 3. **Эффекты при наведении**
- Убраны эффекты трансформации карточки (зум, translateY)
- Оставлен только эффект появления оверлея с информацией о файле
- Убраны градиенты из оверлея

### 4. **Иконки на карточке**

#### Первая иконка (тип файла) - статичная, информативная:
- **Всегда видна** после загрузки изображения
- Показывает формат файла: изображение (📷) или видео (▶️)
- Позиция: верхний левый угол
- Класс: `.card__type-badge`
- Поведение: статична, без интерактивности
- Иконки: 
  - Изображение: `image-16-border`
  - Видео: `play-16-fill`

#### Вторая иконка (мудборд) - интерактивная:
- **Появляется при наведении** курсора на карточку
- **Всегда видна**, если карточка уже добавлена в мудборд
- Позиция: верхний правый угол
- Класс: `.card__moodboard-button`
- Состояния:
  - Обычное: прозрачная, появляется при ховере на карточку
  - При наведении на карточку: `opacity: 1`
  - При наведении на саму кнопку: увеличение масштаба (`scale(1.1)`)
  - Активное (в мудборде): красный фон, всегда видна
- Иконки:
  - Не в мудборде: `bookmark-plus-16-border` (закладка с плюсом)
  - В мудборде: `bookmark-16-border` (обычная закладка)

### 5. **Удаленные элементы**
- Чекбокс выбора карточки (`.card__checkbox`)
- Старый индикатор мудборда (`.card__moodboard-badge`)
- Градиенты в оверлее

---

## 📦 Измененные файлы

1. `renderer/src/components/common/Card.tsx`
2. `renderer/src/components/common/Card.css`
3. `renderer/src/components/layout/Layout.css`
4. `renderer/src/components/gallery/MasonryGrid.tsx` (добавлена поддержка `onMoodboardToggle`)

---

## 🔧 Новый API компонента Card

```typescript
interface CardProps {
  card: CardType;
  compact?: boolean;
  selected?: boolean;
  onClick?: (card: CardType) => void;
  onSelect?: (card: CardType, selected: boolean) => void;
  onMoodboardToggle?: (card: CardType) => void;  // ← НОВЫЙ PROP
  showActions?: boolean;
}
```

### Новый обработчик: `onMoodboardToggle`
- Вызывается при клике на кнопку мудборда
- Параметр: объект карточки
- Родительский компонент должен обрабатывать добавление/удаление из мудборда

---

## 📝 Заметки для разработчиков

1. **Иконка типа файла** теперь показывается для всех карточек, не только для видео
2. **Кнопка мудборда** требует подключения обработчика `onMoodboardToggle` в родительских компонентах
3. **Оверлей** теперь без градиентов, содержит только информацию о файле внизу
4. **Белый фон** контентной области улучшает контраст с карточками

---

## 🎯 Следующие шаги

- [ ] Подключить обработчик `onMoodboardToggle` в компонентах, использующих Card
- [ ] Протестировать поведение иконок на разных типах файлов
- [ ] Проверить компактный режим карточек
- [ ] Провести визуальное тестирование на разных разрешениях (1920×1080 - 2560×1440)

