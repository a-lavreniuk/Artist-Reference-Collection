# Изменения в стилизации карточек

## Дата: 9 ноября 2025
## Ветка: `feature/card-styling`

---

## ✅ Выполненные изменения

### 1. **Скругление углов карточки**
- Установлено значение `border-radius: 16px` (вместо переменной `--radius-m` = 12px)
- Файл: `renderer/src/components/common/Card.css`

### 2. **Фон контентной области**
- Изменен цвет фона `.layout__content` на белый (`background-color: white`)
- Файл: `renderer/src/components/layout/Layout.css`

### 3. **Эффекты при наведении**
- Убраны все эффекты трансформации карточки (зум, translateY)
- **Убран зум изображения внутри карточки** — изображения статичны при ховере
- Убраны эффекты зума на кнопках (иконках)
- Карточка полностью статична при наведении

### 4. **Иконки на карточке**

Обе иконки расположены **в верхнем левом углу рядом друг с другом**:

#### Первая иконка (тип файла) - статичная, информативная:
- **Всегда видна** после загрузки изображения
- Показывает формат файла: изображение (📷) или видео (▶️)
- Позиция: верхний левый угол, первая слева
- Класс: `.card__type-badge`
- Поведение: статична, без интерактивности
- Иконки: 
  - Изображение: `<Icon name="image" size={16} variant="border" />`
  - Видео: `<Icon name="play" size={16} variant="border" />`

#### Вторая иконка (мудборд) - интерактивная:
- **Появляется при наведении** курсора на карточку
- **Всегда видна**, если карточка уже добавлена в мудборд
- Позиция: верхний левый угол, справа от иконки типа файла (с отступом `16px`)
- Класс: `.card__moodboard-button`
- **Функциональность:**
  - **Клик на метку с плюсом** → добавляет карточку в мудборд (без открытия детального просмотра)
  - **Клик на метку с минусом** → удаляет карточку из мудборда (без открытия детального просмотра)
  - `e.stopPropagation()` предотвращает открытие карточки при клике на метку
- Состояния:
  - Обычное: прозрачная, появляется при ховере на карточку
  - При наведении на карточку: `opacity: 1`
  - Активное (в мудборде): всегда видна, фон как у обычной метки
- Иконки:
  - Не в мудборде: `<Icon name="bookmark-plus" size={16} variant="border" />` (закладка с плюсом)
  - В мудборде: `<Icon name="bookmark-minus" size={16} variant="border" />` (закладка с минусом)

#### Стиль фона меток:
```css
background: rgba(81, 79, 91, 0.48);
backdrop-filter: blur(64px);
```

#### Отступы:
- Внутренний отступ карточки: **16px** (расстояние меток от краев карточки)

> **⚠️ Важно**: Компонент `Icon` автоматически формирует имя файла из комбинации `{name}-{size}-{variant}.svg`. Поэтому нужно передавать **только базовое имя** иконки (`"image"`, `"bookmark-plus"`), а размер и вариант — отдельными пропами.

### 5. **Удаленные элементы**
- Чекбокс выбора карточки (`.card__checkbox`)
- Старый индикатор мудборда (`.card__moodboard-badge`)
- Градиенты в оверлее
- Оверлей с названием и весом файла (`.card__info`, `.card__overlay`)

---

## 📦 Измененные файлы

1. `renderer/src/components/common/Card.tsx`
2. `renderer/src/components/common/Card.css`
3. `renderer/src/components/layout/Layout.css`
4. `renderer/src/components/gallery/MasonryGrid.tsx` (добавлена поддержка `onMoodboardToggle`)

---

## 🔧 Новый API компонента Card

```typescript
interface CardProps {
  card: CardType;
  compact?: boolean;
  selected?: boolean;
  onClick?: (card: CardType) => void;
  onSelect?: (card: CardType, selected: boolean) => void;
  onMoodboardToggle?: (card: CardType) => void;  // ← НОВЫЙ PROP
  showActions?: boolean;
}
```

### Новый обработчик: `onMoodboardToggle`
- Вызывается при клике на кнопку мудборда
- Параметр: объект карточки
- Родительский компонент должен обрабатывать добавление/удаление из мудборда
- **Клик на метку работает напрямую** (не требует открытия детального просмотра)
- Событие клика не всплывает до карточки благодаря `e.stopPropagation()`

---

## 📝 Заметки для разработчиков

1. **Иконка типа файла** теперь показывается для всех карточек, не только для видео
2. **Кнопка мудборда** требует подключения обработчика `onMoodboardToggle` в родительских компонентах
3. **Обе иконки** расположены рядом друг с другом в верхнем левом углу с отступом 16px
4. **Оверлей с информацией** о файле (название, вес) удалён
5. **Белый фон** контентной области улучшает контраст с карточками
6. **Фон меток** использует полупрозрачный стиль с blur(64px) для лучшей читаемости
7. **Иконка bookmark-minus** используется для карточек, уже добавленных в мудборд
8. **Убраны все эффекты зума** — ни на изображениях, ни на иконках при наведении
9. **Карточки статичны** при ховере, только иконки меняют прозрачность

---

## 🎯 Следующие шаги

- [x] ~~Подключить обработчик `onMoodboardToggle` в компонентах, использующих Card~~ ✅ Готово
  - [x] CardsPage
  - [x] MoodboardPage
  - [x] CollectionDetailPage
- [ ] Протестировать поведение иконок на разных типах файлов
- [ ] Проверить компактный режим карточек
- [ ] Провести визуальное тестирование на разных разрешениях (1920×1080 - 2560×1440)

