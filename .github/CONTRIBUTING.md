# Руководство по внесению вклада в ARC

Спасибо за интерес к проекту ARC! Мы ценим любой вклад в развитие приложения.

## 🚀 Как начать

1. **Форкните репозиторий**
2. **Клонируйте свой форк:**
   ```bash
   git clone https://github.com/ваш-username/arc.git
   cd arc
   ```
3. **Установите зависимости:**
   ```bash
   npm install
   cd renderer && npm install && cd ..
   ```
4. **Создайте ветку для своих изменений:**
   ```bash
   git checkout -b feature/название-фичи
   ```

## 💻 Процесс разработки

### Структура проекта

- `/src/main/` — код main процесса Electron (TypeScript)
- `/src/preload/` — preload скрипты (TypeScript)
- `/renderer/` — React приложение (TypeScript + Vite)
- `/docs/` — документация
- `/resources/` — ресурсы для сборки (иконки и т.д.)

### Запуск в режиме разработки

```bash
npm run dev
```

Это запустит Vite dev-сервер и Electron одновременно.

### Сборка

```bash
# Собрать TypeScript
npm run build

# Создать установщик
npm run electron:build
```

## 📝 Стандарты кодирования

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`, используйте конкретные типы
- Комментируйте сложную бизнес-логику на русском языке

### React

- Функциональные компоненты + хуки
- Держите компоненты небольшими и сфокусированными
- Используйте TypeScript для props

### Git commits

Используйте понятные сообщения коммитов на русском языке:

```
✅ Хорошо:
- "Добавлена функция экспорта коллекций в ZIP"
- "Исправлена ошибка при удалении тегов"
- "Оптимизирована загрузка превью"

❌ Плохо:
- "fix"
- "update"
- "changes"
```

## 🐛 Сообщение об ошибках

При создании issue, пожалуйста, укажите:

- Версию приложения
- Версию Windows
- Шаги для воспроизведения
- Ожидаемое и фактическое поведение
- Скриншоты (если применимо)

## ✨ Предложение новых функций

Перед началом работы над большой фичей:

1. Создайте issue с описанием
2. Дождитесь обсуждения и одобрения
3. Приступайте к разработке

## 📋 Pull Request Process

1. Убедитесь, что код компилируется без ошибок
2. Протестируйте изменения локально
3. Обновите документацию при необходимости
4. Создайте PR с понятным описанием изменений
5. Дождитесь review и внесите правки при необходимости

## 🤝 Код поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Помогайте новичкам освоиться в проекте

## 📞 Связь

Если у вас есть вопросы, создайте issue или свяжитесь с мейнтейнерами.

---

Спасибо за ваш вклад! 🎉

