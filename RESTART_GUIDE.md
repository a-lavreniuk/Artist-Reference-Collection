# 🔄 Полная инструкция по перезапуску приложения

## 📖 Что такое dev-сервер?

**Dev-сервер** (development server) — это программа, которая запускает ваше приложение в режиме разработки. Когда вы ранее выполняли команду `npm run dev`, запустился локальный сервер, который:
- Открыл приложение в браузере по адресу `http://localhost:3000`
- Следит за изменениями в коде
- Автоматически обновляет страницу при изменениях

Этот сервер работает в **терминале (командной строке)** и должен быть **остановлен** перед перезапуском.

---

## 🛑 Шаг 1: Остановка dev-сервера

### Вариант А: Если терминал виден в Cursor

1. **Найдите окно терминала** в нижней части Cursor:
   - Там должна быть вкладка **TERMINAL**
   - В терминале бегущий текст типа:
     ```
     VITE v5.x.x  ready in xxx ms
     ➜  Local:   http://localhost:3000/
     ➜  press h + enter to show help
     ```

2. **Кликните** в окно терминала (чтобы курсор был там)

3. **Нажмите** на клавиатуре:
   ```
   Ctrl + C
   ```
   (Зажмите клавишу `Ctrl` и не отпуская её нажмите `C`)

4. ✅ Сервер остановлен если:
   - Появилась строка с `PS C:\GitHub\arc>`
   - Курсор моргает и ждёт команды
   - В браузере приложение перестало работать

### Вариант Б: Если терминал не виден

1. **Откройте панель терминала:**
   - Нажмите `` Ctrl + ` `` (клавиша тильда, слева от 1)
   - Или в меню: `View` → `Terminal`

2. **Проверьте** есть ли запущенный процесс:
   - Если в терминале бегущий текст и `http://localhost:3000/` — сервер запущен
   - Нажмите `Ctrl + C` чтобы остановить

3. Если терминал пустой, возможно сервер уже остановлен ✅

### Вариант В: Если не получается остановить

1. **Закройте** Cursor полностью (Alt + F4)
2. **Откройте** Cursor заново
3. Dev-сервер автоматически остановится

---

## 🗑️ Шаг 2: Очистка базы данных

Это **критически важно**, чтобы удалить старые данные с неправильными путями к файлам!

### Как очистить IndexedDB:

1. **Откройте браузер** с приложением (если закрыт — не страшно, откроем потом)

2. **Откройте инструменты разработчика:**
   - Нажмите `F12` на клавиатуре
   - Или правый клик на странице → "Inspect" (Проверить)
   - Или `Ctrl + Shift + I`

3. **Перейдите во вкладку Application:**
   ```
   [Elements] [Console] [Sources] ... [Application] ← Эта вкладка
   ```
   (Если не видите, нажмите `>>` чтобы раскрыть больше вкладок)

4. **В левой панели** найдите раздел **Storage:**
   ```
   Storage
     ├── Local Storage
     ├── Session Storage
     ├── IndexedDB          ← Раскройте этот раздел
     │   ├── ARC_DB         ← Удалить
     │   └── ARC_FileHandles ← Удалить
     ├── Cookies
     └── ...
   ```

5. **Удалите базы данных:**
   - **Правый клик** на `ARC_DB` → **Delete database**
   - **Правый клик** на `ARC_FileHandles` → **Delete database**

6. ✅ Готово! Можете закрыть инструменты разработчика (F12)

### Альтернативный способ (если не получается):

1. В инструментах разработчика (F12)
2. Вкладка **Application**
3. В левой панели: **Storage** → **Clear site data**
4. Поставьте галочку **Indexed DB**
5. Нажмите **Clear site data**
6. ✅ Готово!

---

## ▶️ Шаг 3: Запуск обновлённого приложения

### Запуск через терминал в Cursor:

1. **Убедитесь** что терминал открыт:
   - `` Ctrl + ` `` (открыть терминал)

2. **Перейдите в папку проекта** (если ещё не там):
   ```powershell
   cd C:\GitHub\arc\arc-app
   ```
   Нажмите `Enter`

3. **Запустите dev-сервер:**
   ```powershell
   npm run dev
   ```
   Нажмите `Enter`

4. **Ждите** пока появится:
   ```
   VITE v5.x.x  ready in xxx ms

   ➜  Local:   http://localhost:3000/
   ➜  Network: use --host to expose
   ➜  press h + enter to show help
   ```

5. ✅ Сервер запущен!

### Что делать если появились ошибки:

#### Ошибка: "npm not found"
```powershell
# Проверьте что Node.js установлен:
node --version

# Должно показать версию, например: v20.x.x
# Если ошибка — переустановите Node.js
```

#### Ошибка: "Cannot find module"
```powershell
# Установите зависимости заново:
npm install
```

#### Ошибка: "Port 3000 is already in use"
```powershell
# Порт занят, нужно найти и убить процесс:
# Вариант 1 - использовать другой порт:
npm run dev -- --port 3001

# Вариант 2 - убить процесс на порту 3000:
netstat -ano | findstr :3000
# Найдите PID (последнее число)
# Затем:
taskkill /PID <номер_PID> /F
```

---

## 🌐 Шаг 4: Открытие приложения в браузере

1. **Автоматически:**
   - При запуске `npm run dev` браузер должен открыться автоматически

2. **Вручную:**
   - Откройте браузер (Chrome, Edge, Яндекс Браузер)
   - Введите в адресную строку: `http://localhost:3000`
   - Нажмите `Enter`

3. ✅ Приложение должно загрузиться!

---

## 🎯 Шаг 5: Первый запуск (онбординг)

После очистки базы данных появится **экран приветствия**:

### Что вы увидите:
```
╔════════════════════════════════════╗
║  Добро пожаловать в ARC            ║
║                                    ║
║  Выберите рабочую папку для        ║
║  хранения ваших медиафайлов        ║
║                                    ║
║  [Выбрать папку]  [Пропустить]     ║
╚════════════════════════════════════╝
```

### Что нужно сделать:

1. **Нажмите "Выбрать папку"**

2. **Выберите папку** где будут храниться файлы:
   - Создайте новую папку, например: `C:\ARC_Media`
   - Или выберите существующую папку с референсами
   - **ВАЖНО:** Приложение будет работать ТОЛЬКО с этой папкой

3. **Дайте разрешение:**
   - Браузер спросит: "Allow this site to view files?"
   - Нажмите **"View files"** или **"Разрешить"**

4. ✅ Готово! Вы в приложении!

---

## ✅ Проверка что всё работает

### Тест 1: Создание категории

1. Перейдите в **"Категории и метки"** (в боковой панели слева)
2. Нажмите **"+ Создать категорию"**
3. Введите название, например: "Персонажи"
4. Выберите цвет
5. Нажмите **"Создать"**
6. ✅ Категория должна появиться

### Тест 2: Создание метки

1. В созданной категории нажмите **"+ Новая метка"**
2. Введите название, например: "Девушка"
3. Нажмите **"Добавить"**
4. ✅ Метка должна появиться в категории

### Тест 3: Добавление карточки

1. Перейдите в **"Добавить карточки"** (кнопка + в верхней панели)
2. Перетащите 1-2 изображения в зону Drag & Drop
3. Или нажмите на зону чтобы выбрать файлы
4. Выберите метки для каждого изображения (обязательно!)
5. Нажмите **"Далее"** → **"Завершить"**
6. ✅ Должно появиться: **"💾 Сохранение файлов в рабочую папку..."**
7. ✅ Затем: **"✅ Добавлено 2 карточек!"**

### Тест 4: Проверка файлов в папке

1. Откройте **Проводник Windows**
2. Перейдите в выбранную рабочую папку (например `C:\ARC_Media`)
3. ✅ Там должны быть скопированные файлы!

### Тест 5: Проверка галереи

1. Перейдите в **"Все карточки"**
2. ✅ Должны отображаться загруженные изображения
3. Кликните на карточку
4. ✅ Должно открыться модальное окно с превью

---

## 🆘 Частые проблемы и решения

### Проблема: Белый экран
**Решение:**
- Откройте консоль (F12 → вкладка Console)
- Проверьте есть ли ошибки красным цветом
- Напишите мне какие ошибки видите

### Проблема: "Не удалось загрузить" в карточках
**Решение:**
- Вы не очистили IndexedDB
- Вернитесь к **Шагу 2** и удалите базы данных
- Перезагрузите страницу (F5)

### Проблема: Карточки не сохраняются
**Решение:**
- Проверьте что выбрали рабочую папку при онбординге
- Перейдите в Настройки → проверьте статус папки
- Если "Папка не выбрана" — нажмите "Выбрать папку"

### Проблема: Интерфейс пропадает при переключении разделов
**Решение:**
- Это значит произошла ошибка
- Откройте консоль (F12)
- Должно показаться красивое сообщение об ошибке
- Нажмите "Попробовать снова"
- Если не помогло — напишите мне ошибку

### Проблема: Терминал не реагирует на Ctrl+C
**Решение:**
- Закройте терминал (нажмите X на вкладке TERMINAL)
- Откройте новый терминал (Ctrl + `)
- Или перезапустите Cursor

---

## 📋 Краткая шпаргалка

### Для остановки сервера:
```
1. Кликнуть в терминал
2. Нажать Ctrl + C
```

### Для запуска сервера:
```powershell
cd C:\GitHub\arc\arc-app
npm run dev
```

### Для очистки базы:
```
1. F12
2. Application → IndexedDB
3. Удалить ARC_DB и ARC_FileHandles
```

### Для перезапуска всего:
```powershell
# 1. Остановить (если запущен)
Ctrl + C

# 2. Перейти в папку
cd C:\GitHub\arc\arc-app

# 3. Запустить
npm run dev

# 4. Открыть браузер
http://localhost:3000
```

---

## 🎯 Готово!

После выполнения всех шагов у вас будет:
- ✅ Чистая база данных
- ✅ Обновлённый код
- ✅ Работающее сохранение файлов
- ✅ Стабильный интерфейс без падений

**Если что-то не получается — напиши мне на каком шаге застрял, помогу!** 🚀

