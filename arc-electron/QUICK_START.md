# 🚀 Быстрый старт - 5 минут до запуска

## Шаг 1: Проверьте установку зависимостей

```bash
cd C:\GitHub\arc\arc-electron

# Проверьте корневые зависимости
npm list --depth=0

# Проверьте renderer зависимости
cd renderer
npm list --depth=0
cd ..
```

Если что-то не установлено:
```bash
npm install
cd renderer && npm install && cd ..
```

## Шаг 2: Запустите приложение

### Вариант А: Автоматический (рекомендуется)

Просто дважды кликните на файл:
```
start-dev.bat
```

### Вариант Б: Через командную строку

```bash
cd C:\GitHub\arc\arc-electron
start-dev.bat
```

## Что произойдёт?

1. Откроется терминал с Vite dev-сервером
2. Через 5 секунд откроется окно Electron с приложением
3. DevTools откроются автоматически (для отладки)

## Первое использование

1. **Выберите рабочую папку** или нажмите "Пропустить"
   - Откроется Windows диалог выбора папки
   - Выберите любую пустую папку
   - Путь сохранится автоматически

2. **Создайте категории и метки**
   - Перейдите в раздел "Метки" (боковое меню)
   - Нажмите "Добавить категорию"
   - Добавьте несколько меток

3. **Попробуйте добавить файлы**
   - Перейдите в раздел "Добавить"
   - Drag & Drop пока работает частично (требует доработки)
   - Можно выбрать файлы через кнопку

4. **Просмотрите галерею**
   - Вернитесь на страницу "Карточки"
   - Попробуйте фильтры и поиск

## Горячие клавиши

- `Ctrl+N` - Создать карточку
- `Ctrl+F` - Открыть поиск
- `Delete` - Удалить выбранное
- `Ctrl+,` - Настройки
- `Ctrl+M` - Мудборд
- `F5` - Обновить
- `F12` - DevTools

## Если что-то не работает

### Окно не открывается
1. Проверьте терминал на ошибки
2. Убедитесь что порт 5173 свободен
3. Попробуйте закрыть все Electron процессы и запустить заново

### "Electron API недоступен"
1. Проверьте что приложение запущено через `start-dev.bat`
2. Откройте DevTools (F12)
3. В консоли введите: `window.electronAPI`
4. Должен вывести объект с функциями

### React не загружается
1. Закройте Electron
2. Запустите только Vite: `cd renderer && npm run dev`
3. Откройте http://localhost:5173 в браузере
4. Если работает в браузере - проблема в Electron
5. Если не работает - проблема в React коде

## Остановка приложения

1. Закройте окно Electron (или Ctrl+W)
2. В терминале Vite нажмите Ctrl+C
3. Подтвердите остановку: Y

## Сборка установщика (опционально)

Когда всё работает и хотите создать .exe:

```bash
cd C:\GitHub\arc\arc-electron
npm run build
npm run electron:build
```

Результат: `release/ARC-Setup-1.0.0.exe`

---

**Готово! Приятного использования ARC! 🎉**

