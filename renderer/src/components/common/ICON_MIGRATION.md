# Миграция компонента Icon на загрузку из SVG файлов

## ✅ Что сделано

### 1. Новый подход
- **Раньше:** SVG paths были захардкожены в компоненте (900+ строк кода)
- **Теперь:** SVG загружаются динамически из файлов `renderer/src/assets/icons/`

### 2. Автоматическая замена цветов
Компонент автоматически заменяет черный цвет на `currentColor`:
```javascript
stroke="black" → stroke="currentColor"
fill="black" → fill="currentColor"
```

### 3. Маппинг специальных имен
Некоторые файлы имеют нестандартные названия:
- `trash-*-fill` → `trash-3-*-fill.svg`
- `trash-16-border` → `trash--16-border.svg` (двойной дефис)
- `grid-*-fill` → `grid-2-*-fill.svg`

### 4. Встроенные SVG
Для иконок, которых нет в файлах:
- `arrow-left` (все варианты)
- `search` (все варианты)

## 📁 Структура файлов

```
renderer/src/assets/icons/
├── folder-24-border.svg
├── folder-24-fill.svg
├── folder-16-border.svg
├── folder-16-fill.svg
├── tag-24-border.svg
└── ... (~140 файлов)
```

## 🎨 Как работают цвета

### 1. В SVG файлах
```svg
<!-- Исходный файл (черный) -->
<svg viewBox="0 0 24 24">
  <path stroke="black" fill="none"/>
</svg>

<!-- После загрузки (автозамена) -->
<svg viewBox="0 0 24 24">
  <path stroke="currentColor" fill="none"/>
</svg>
```

### 2. В CSS контексте
```tsx
// Иконка наследует цвет из родителя
<button style={{ color: 'red' }}>
  <Icon name="folder" size={24} variant="border" />
  {/* Иконка будет красной */}
</button>
```

### 3. Переменные из палитры
```css
/* Sidebar - серые иконки */
.sidebar__icon {
  color: var(--icon-default); /* #93919A */
}

/* При hover - черные */
.sidebar__item:hover .sidebar__icon {
  color: var(--text-primary); /* #3B3946 */
}
```

## 🚀 Использование

Ничего не изменилось в API компонента:

```tsx
import { Icon } from '../common';

// Как и раньше
<Icon name="folder" size={24} variant="border" />
<Icon name="tag" size={16} variant="fill" />
```

## 🔧 Добавление новых иконок

### Способ 1: Добавить SVG файл (рекомендуется)

1. Экспортируйте иконку из Figma в SVG
2. Назовите файл: `[name]-[size]-[style].svg`
3. Убедитесь что используется `stroke="black"` или `fill="black"`
4. Положите файл в `renderer/src/assets/icons/`
5. Добавьте название в тип `IconName` (если новая иконка)

### Способ 2: Добавить встроенный SVG

Если файл невозможно добавить, отредактируйте `Icon.tsx`:

```typescript
const inlineSvgs: Record<string, string> = {
  'new-icon-24-border': '<svg viewBox="0 0 24 24">...</svg>',
  // ... остальные
};
```

## 📊 Производительность

### Кэширование
- Загруженные SVG кэшируются в памяти
- Повторное использование иконки = мгновенная загрузка

### Оптимизация сборки
- Vite автоматически оптимизирует SVG при сборке
- Минификация и tree-shaking работают автоматически

## ⚠️ Важные замечания

### 1. Асинхронная загрузка
Иконки загружаются асинхронно, но очень быстро:
- При первом рендере показывается заглушка (span с размерами)
- После загрузки SVG вставляется в DOM
- Кэш гарантирует что повторная загрузка мгновенная

### 2. dangerouslySetInnerHTML
Используется для вставки SVG, но это безопасно так как:
- SVG контролируется нами (из наших файлов)
- Автоматическая замена цветов - единственная модификация
- Нет пользовательского ввода

### 3. currentColor
Все SVG ДОЛЖНЫ использовать `currentColor`:
- Компонент делает автозамену, но лучше сразу экспортировать правильно
- Без `currentColor` иконки будут всегда черными

## 🐛 Отладка

### Иконка не загружается
1. Откройте DevTools Console
2. Ищите ошибку `[Icon] Не найден файл: ...`
3. Проверьте название файла и путь
4. Убедитесь что файл существует

### Иконка черная, не меняет цвет
1. Откройте DevTools → Elements
2. Найдите SVG внутри `.icon`
3. Проверьте что в SVG `stroke="currentColor"` или `fill="currentColor"`
4. Если нет - файл нужно обновить или компонент не заменил цвет

### Иконка мигает при первой загрузке
Это нормально - асинхронная загрузка. Но:
- Повторная загрузка будет мгновенной (кэш)
- Можно добавить preload в будущем

## 📈 Статистика

**Раньше:**
- ~928 строк кода в Icon.tsx
- ~40 иконок захардкожены
- Сложно обновлять/добавлять новые

**Теперь:**
- ~193 строки кода в Icon.tsx  
- ~140 SVG файлов в папке
- Легко добавить/обновить - просто замените файл

## 🎯 Следующие шаги

1. **Протестируйте приложение:**
   - Все иконки должны загружаться
   - Цвета должны работать правильно
   - Нет ошибок в консоли

2. **Добавьте недостающие иконки:**
   - `arrow-left` (если есть в Figma)
   - `search` (если есть в Figma)
   - Любые другие варианты

3. **Оптимизируйте SVG файлы:**
   - Можно прогнать через SVGO
   - Убрать лишние атрибуты
   - Но текущие файлы уже работают!

