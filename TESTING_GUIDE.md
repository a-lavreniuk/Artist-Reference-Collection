# Руководство по тестированию исправлений
## Ветка: bugfix/critical-issues

**Коммит:** d8b0351  
**Дата:** 3 декабря 2025

---

## 🚀 Запуск приложения

### Режим разработки (с DevTools)

```bash
# В корневой директории
npm run dev
```

**Что происходит:**
- Запускается Vite dev server (порт 5173)
- Открывается Electron приложение
- **DevTools открываются автоматически** ✨
- Hot reload работает для renderer процесса

**Горячие клавиши:**
- `Ctrl+Shift+I` - открыть/закрыть DevTools
- `Ctrl+R` - перезагрузить renderer
- `Ctrl+N` - добавить карточку
- `Ctrl+F` - поиск
- `Ctrl+M` - мудборд
- `F5` - обновить галерею

---

## 🧪 Тест A: Импорт из браузера

### Установка расширения

**Chrome/Edge:**
1. Откройте `chrome://extensions/` или `edge://extensions/`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `browser-extension/`

### Тестирование

1. **Откройте любую страницу с изображениями** (например, Pinterest, ArtStation)
2. **Наведите на изображение** → должна появиться кнопка "Сохранить в ARC"
3. **Нажмите кнопку** → браузер откроет приложение ARC
4. **Проверьте консоль DevTools** на наличие логов:
   ```
   [MAIN] Обработка протокола: arc://import?url=...
   [DownloadManager] Начало загрузки: https://...
   [DownloadManager] Файл успешно загружен
   [DownloadManager] Событие отправлено в renderer
   [App] Получено событие загрузки файла: {...}
   ```
5. **Должен появиться Toast** с предложением добавить файл
6. **Нажмите "Добавить"** → откроется страница `/add` с файлом

### Что может пойти не так

**Если Toast не появился:**
- Проверьте консоль на ошибки
- Убедитесь что рабочая папка настроена
- Проверьте что папка `_cache/imports` создана
- Проверьте что событие `main:external-file-downloaded` отправляется

**Если файл не загрузился:**
- Проверьте консоль main process (в логах)
- Возможно CORS блокирует загрузку
- Попробуйте другое изображение (с прямой ссылкой)

---

## 🧪 Тест D: Меню и обновления

### Системное меню

1. **Нажмите Alt** → должно появиться меню
2. **Проверьте разделы:**
   - Только **1 раздел "ARC"** вместо 4
   - Минималистичный дизайн
   - Все горячие клавиши работают

### Кнопка обновлений

1. **Откройте настройки** (Ctrl+,)
2. **В правом нижнем углу** должна быть:
   - Кнопка "Проверить обновления"
   - Badge с версией "v1.2.8"
3. **Нажмите "Проверить обновления"**:
   - Кнопка меняется на "Проверка..."
   - Появляется alert "Проверка запущена..."
4. **Проверьте консоль DevTools:**
   ```
   [IPC] Проверка обновлений...
   [AutoUpdater] Проверка обновлений...
   ```

### Если обновление доступно

- Появится компонент `UpdateNotification` вверху экрана
- Кнопка "Обновить" → устанавливает обновление

---

## 🧪 Полный тест-набор

### 1. Первый запуск
- [ ] Онбординг показывается
- [ ] Выбор рабочей папки работает
- [ ] Папки `_cache/imports` и `_cache/thumbs` создаются

### 2. Импорт карточек
- [ ] Drag & drop работает
- [ ] Выбор файлов через диалог работает
- [ ] Импорт из браузера работает (расширение)
- [ ] Превью генерируются для изображений
- [ ] Превью генерируются для видео (ffmpeg)

### 3. Работа с данными
- [ ] Создание тегов и категорий
- [ ] Поиск по тегам работает
- [ ] Добавление в мудборд
- [ ] Удаление карточек (файлы тоже удаляются)

### 4. Системные функции
- [ ] Backup создается (проверьте размер в MB)
- [ ] Restore восстанавливает данные
- [ ] Перенос рабочей папки работает
- [ ] Экспорт файлов работает
- [ ] Проверка целостности находит проблемы

### 5. Горячие клавиши
- [ ] Ctrl+N → добавить карточку
- [ ] Ctrl+F → поиск
- [ ] Ctrl+M → мудборд
- [ ] F5 → обновить
- [ ] Ctrl+, → настройки
- [ ] Ctrl+Shift+I → DevTools

### 6. Логи и отладка
- [ ] Логи пишутся в `%APPDATA%\arc-electron\main-process.log`
- [ ] При достижении 10MB лог ротируется
- [ ] Хранится максимум 5 старых логов
- [ ] DevTools открываются/закрываются по Ctrl+Shift+I

---

## 🐛 Отладка

### Открыть папку с логами

**Через UI:**
- Настройки → прокрутите вниз → кнопка "Открыть папку с логами"

**Вручную:**
```
%APPDATA%\arc-electron\
```

**Файлы логов:**
- `main-process.log` - текущий лог
- `main-process-YYYY-MM-DD.log` - архивные логи
- `settings.json` - настройки приложения

### Консоль разработчика

**Открыть:**
- `Ctrl+Shift+I` (горячая клавиша)
- Меню → ARC → Консоль разработчика

**Что смотреть:**
- Вкладка Console - логи renderer процесса
- Вкладка Network - запросы (если есть)
- Вкладка Application → IndexedDB - состояние базы данных

### Проверка базы данных

**В консоли DevTools:**
```javascript
// Получить все карточки
await window.db.cards.toArray()

// Проверить мудборд
await window.db.moodboard.get('default')

// Проверить конкретную карточку
await window.debugCard('card-id-here')

// Посмотреть статистику
await window.db.cards.count()
await window.db.tags.count()
```

---

## 📊 Ожидаемые результаты

### ✅ Исправлено в этой ветке

| Проблема | Было | Стало |
|----------|------|-------|
| download-manager.ts | ❌ Отсутствует | ✅ Создан (263 строки) |
| Типы ElectronAPI | ❌ Не синхронизированы | ✅ Синхронизированы |
| Windows пути | ❌ `workingDir + '\\' + path` | ✅ `pathJoin(workingDir, path)` |
| globalShortcut | ❌ Delete перехватывается глобально | ✅ Menu accelerators (локально) |
| check-for-updates | ❌ TODO заглушка | ✅ Реализован |
| Валидация путей | ❌ Отсутствует | ✅ Path traversal защита |
| Repo name | ❌ `arc` | ✅ `Artist-Reference-Collection` |
| Логи | ❌ Бесконечный рост | ✅ Ротация (10MB max) |
| DevTools | ❌ Закомментировано | ✅ Открывается автоматически |
| Меню обновлений | ❌ Системный dialog | ✅ UI кнопка в настройках |

---

## 🎯 Критерии успеха

### Минимальные требования
- ✅ Приложение **запускается** без ошибок
- ✅ TypeScript **компилируется** без ошибок
- ✅ Импорт карточек **работает**
- ✅ Поиск и фильтры **работают**

### Расширенные требования
- ✅ Импорт из браузера **работает**
- ✅ DevTools **открывается**
- ✅ Горячие клавиши **работают локально**
- ✅ Кнопка обновлений **в UI**

---

## 🔄 Следующие шаги

После успешного тестирования:

```bash
# 1. Убедиться что все работает
npm run dev

# 2. Собрать production build
npm run build:prod

# 3. Протестировать installer
# release/ARC-Setup-1.2.8.exe

# 4. Если все ОК - мержить в main
git checkout main
git merge bugfix/critical-issues
git push origin main

# 5. Создать release (опционально)
git tag v1.2.9
git push origin v1.2.9
```

---

**Конец руководства**

