# ARC v1.2.7 — Управление порядком категорий

## Что нового

### Кнопки управления порядком категорий

Теперь вы можете изменять порядок категорий на странице "Категории и метки" с помощью удобных кнопок "вверх" и "вниз". Порядок категорий сохраняется и применяется на всех страницах приложения.

**Основные возможности:**
- ⬆️ **Кнопка "Вверх"** — переместить категорию выше в списке
- ⬇️ **Кнопка "Вниз"** — переместить категорию ниже в списке
- 💾 **Автоматическое сохранение** — порядок сохраняется мгновенно
- 🔄 **Глобальное применение** — порядок категорий используется везде: на странице добавления карточки, в модальном окне редактирования, в фильтрах

**Где использовать:**
- Расположите самые часто используемые категории в начале списка
- Сгруппируйте связанные категории рядом друг с другом
- Создайте логичную структуру для вашего рабочего процесса

## Улучшения интерфейса

- 📐 **Объединение блоков** — блоки "Шаблон" и "Описание" объединены по горизонтали (50:50) для более эффективного использования пространства
- 🎯 **Упрощение навигации** — убраны кнопки навигации в очереди добавления карточек (переключение теперь осуществляется через клик по превью)
- 🔤 **Алфавитная сортировка меток** — метки внутри каждой категории автоматически сортируются по алфавиту (а→я, a→z)
- 🎨 **Улучшенная визуальная структура** — обновлены скругления углов у карточек в компактном отображении, улучшена визуальная структура блоков категорий и меток

## Технические детали

- Добавлено поле `order` в схему категорий для хранения порядка
- Реализованы методы `onMoveUp` и `onMoveDown` в компоненте `CategorySection`
- Логика перемещения категорий с обменом значений `order` между соседними элементами
- Добавлены новые иконки `grip-vertical` для визуального обозначения возможности перемещения
- Порядок категорий применяется через сортировку по полю `order` во всех компонентах

## Установка

Скачайте установщик из [Releases](https://github.com/a-lavreniuk/Artist-Reference-Collection/releases) и запустите **ARC Setup 1.2.7.exe**.

Приложение автоматически обновится при следующем запуске (если включены автообновления).

---

**Полная история изменений:** [CHANGELOG.md](CHANGELOG.md)

