# 🔧 Инструкция по внесению изменений в ARC

## 📋 Оглавление

1. [Git Workflow](#git-workflow)
2. [Создание новой функции](#создание-новой-функции)
3. [Тестирование изменений](#тестирование-изменений)
4. [Объединение с основной веткой](#объединение-с-основной-веткой)
5. [Откат изменений](#откат-изменений)
6. [Best Practices](#best-practices)
7. [Работа с Cursor AI](#работа-с-cursor-ai)

---

## 🌿 Git Workflow

### Структура веток

```
main (production)     ← стабильная версия, готовая к использованию
  ↓
feature/название      ← разработка новой функции
  ↓
test                  ← тестирование перед слиянием (опционально)
```

### Основные правила

✅ **ВСЕГДА** работай в отдельной ветке для новых функций  
✅ **ВСЕГДА** тестируй перед слиянием с `main`  
✅ **НИКОГДА** не коммить напрямую в `main` незатестированный код  
✅ **НИКОГДА** не использовать `--force` для `main`

---

## 🚀 Создание новой функции

### Шаг 1: Создай ветку для функции

```bash
# Убедись что ты в main
git checkout main

# Обнови main до последней версии
git pull origin main

# Создай новую ветку (название должно быть понятным)
git checkout -b feature/название-функции

# Примеры хороших названий:
# feature/export-moodboard
# feature/search-suggestions  
# feature/thumbnail-generation
# fix/card-delete-bug
```

### Шаг 2: Работай над функцией

```bash
# Делай изменения в коде
# Тестируй локально с npm run dev

# Коммить небольшими логическими порциями
git add .
git commit -m "feat: описание изменения"

# Примеры хороших коммитов:
# feat: добавлен экспорт мудборда в PNG
# fix: исправлена ошибка удаления карточки
# refactor: оптимизирована загрузка превью
# docs: обновлена документация API
```

### Шаг 3: Отправь ветку на GitHub

```bash
# Первая отправка ветки
git push -u origin feature/название-функции

# Последующие отправки
git push
```

---

## 🧪 Тестирование изменений

### Локальное тестирование

**1. Dev-сервер:**
```bash
cd arc-app
npm run dev
```

**Проверь:**
- ✅ Функция работает как ожидается
- ✅ Нет ошибок в консоли браузера (F12)
- ✅ Не сломался существующий функционал
- ✅ UI выглядит правильно на 1920×1080 и 2560×1440
- ✅ Данные сохраняются в IndexedDB

**2. Production build:**
```bash
npm run build
npm run preview
```

**Проверь:**
- ✅ Сборка проходит без ошибок
- ✅ Приложение работает в production режиме
- ✅ Service Worker регистрируется корректно

### Тестовая ветка (опционально)

Если изменения большие, создай тестовую ветку:

```bash
# Создай test ветку из feature
git checkout -b test
git push -u origin test

# Попроси кого-то протестировать или
# Протестируй на другом компьютере/браузере
```

---

## 🔀 Объединение с основной веткой

### Вариант 1: Простое слияние (рекомендуется)

```bash
# 1. Вернись в main
git checkout main

# 2. Обнови main (на случай если были изменения)
git pull origin main

# 3. Объедини feature ветку
git merge feature/название-функции

# 4. Если есть конфликты - разреши их:
# - Открой файлы с конфликтами
# - Выбери нужный вариант кода
# - Удали маркеры конфликта (<<<<<<, ======, >>>>>>)
git add .
git commit -m "merge: объединение feature/название-функции"

# 5. Отправь на GitHub
git push origin main

# 6. Удали feature ветку (опционально)
git branch -d feature/название-функции
git push origin --delete feature/название-функции
```

### Вариант 2: Pull Request на GitHub

Если хочешь код-ревью или документацию изменений:

```bash
# 1. Отправь feature ветку на GitHub
git push origin feature/название-функции

# 2. Зайди на GitHub: https://github.com/a-lavreniuk/arc

# 3. Нажми "Compare & pull request"

# 4. Заполни описание:
#    - Что изменено
#    - Зачем
#    - Что протестировано

# 5. Нажми "Create pull request"

# 6. Проверь, протестируй

# 7. Нажми "Merge pull request"

# 8. Подтверди слияние

# 9. Обнови локальный main:
git checkout main
git pull origin main
```

---

## ⏮️ Откат изменений

### Если еще не запушил (изменения только локально)

```bash
# Откатить последний коммит (изменения останутся)
git reset --soft HEAD~1

# Откатить последний коммит (изменения удалятся)
git reset --hard HEAD~1

# Откатить конкретный файл
git checkout -- путь/к/файлу
```

### Если уже запушил в feature ветку

```bash
# Вариант 1: Откат последнего коммита
git revert HEAD
git push

# Вариант 2: Полный откат к предыдущему состоянию
git reset --hard HEAD~1
git push --force origin feature/название-функции
# ⚠️ --force можно использовать только для feature веток!
```

### Если уже слил в main (КРИТИЧЕСКАЯ ОШИБКА)

```bash
# 1. Найди хеш коммита до слияния
git log --oneline

# 2. Создай revert коммит
git revert <хеш-коммита-слияния> -m 1

# 3. Запуш изменения
git push origin main

# ⚠️ НЕ используй git reset --hard для main!
```

---

## ✨ Best Practices

### Названия коммитов

Используй стандарт [Conventional Commits](https://www.conventionalcommits.org/):

```
feat:     - новая функция
fix:      - исправление бага
refactor: - рефакторинг кода (без изменения функционала)
docs:     - изменения в документации
style:    - форматирование, пробелы (не влияет на код)
test:     - добавление тестов
chore:    - обновление зависимостей, конфигурации
```

**Примеры:**
```bash
git commit -m "feat: добавлен экспорт мудборда в PNG"
git commit -m "fix: исправлена ошибка при удалении карточки"
git commit -m "refactor: оптимизирована загрузка превью"
git commit -m "docs: обновлена инструкция по установке"
```

### Размер коммитов

✅ **ХОРОШО:**
- Один коммит = одна логическая единица изменений
- Можно легко понять что изменилось
- Можно откатить без проблем

❌ **ПЛОХО:**
- 100+ файлов в одном коммите
- Смешивание разных функций
- Коммиты типа "fixes", "wip", "asdf"

### Частота коммитов

```bash
# Коммить часто!
# Плохо:
git commit -m "done"  # после недели работы

# Хорошо:
git commit -m "feat: добавлен UI экспорта"
# ... работаешь дальше ...
git commit -m "feat: реализована логика экспорта"
# ... работаешь дальше ...  
git commit -m "docs: добавлена документация экспорта"
```

---

## 🤖 Работа с Cursor AI

### Попроси AI создать ветку

```
"Создай новую ветку feature/export-moodboard и переключись на неё"
```

### Попроси AI сделать изменения

```
"Добавь функцию экспорта мудборда в PNG. 
Создай кнопку на странице мудборда, 
которая вызывает функцию экспорта."
```

### Попроси AI протестировать

```
"Запусти dev-сервер и проверь что функция экспорта работает"
```

### Попроси AI слить ветки

```
"Протестируй изменения, затем объедини feature/export-moodboard с main"
```

### Полезные команды для AI

```
"Покажи статус Git"
"Создай коммит с описанием изменений"
"Покажи список веток"
"Отправь изменения на GitHub"
"Создай pull request"
```

---

## 📝 Чеклист перед слиянием с main

Перед `git merge` в `main` убедись:

- [ ] ✅ Функция полностью работает
- [ ] ✅ Протестировано в dev режиме
- [ ] ✅ Production build создаётся без ошибок
- [ ] ✅ Нет ошибок в консоли браузера
- [ ] ✅ Нет TypeScript ошибок
- [ ] ✅ Не сломался существующий функционал
- [ ] ✅ Все файлы закоммичены
- [ ] ✅ Коммиты имеют понятные описания
- [ ] ✅ Обновлена документация (если нужно)
- [ ] ✅ Main ветка обновлена до последней версии

---

## 🎯 Типичные сценарии

### Сценарий 1: Добавление новой функции

```bash
# 1. Создать ветку
git checkout main
git pull origin main
git checkout -b feature/new-function

# 2. Разработка
# ... пишешь код ...
npm run dev  # тестируешь

# 3. Коммиты
git add .
git commit -m "feat: добавлена новая функция"

# 4. Тестирование
npm run build
npm run preview

# 5. Слияние
git checkout main
git merge feature/new-function
git push origin main

# 6. Очистка
git branch -d feature/new-function
```

### Сценарий 2: Исправление бага

```bash
# 1. Создать ветку от main
git checkout main
git checkout -b fix/bug-description

# 2. Исправить
# ... фиксишь баг ...
git add .
git commit -m "fix: исправлен баг с описанием"

# 3. Срочное слияние (если критично)
git checkout main
git merge fix/bug-description
git push origin main
```

### Сценарий 3: Экспериментальная функция

```bash
# 1. Создать ветку
git checkout -b experiment/new-idea

# 2. Экспериментировать
# ... пробуешь разные варианты ...
git commit -m "wip: эксперимент 1"
git commit -m "wip: эксперимент 2"

# 3А. Если получилось - слить с main
git checkout main
git merge experiment/new-idea

# 3Б. Если не получилось - удалить ветку
git checkout main
git branch -D experiment/new-idea
```

---

## 🆘 Помощь при проблемах

### Забыл переключиться на feature ветку

```bash
# Создай ветку прямо сейчас (изменения сохранятся)
git checkout -b feature/название-функции
```

### Случайно изменил main

```bash
# Если еще не закоммитил:
git stash                                    # сохранить изменения
git checkout -b feature/название-функции    # создать ветку
git stash pop                                # применить изменения

# Если уже закоммитил но не запушил:
git branch feature/название-функции          # создать ветку на текущем коммите
git reset --hard HEAD~1                      # откатить main на 1 коммит назад
git checkout feature/название-функции        # переключиться на feature
```

### Конфликты при слиянии

```bash
# 1. Git покажет файлы с конфликтами
git status

# 2. Открой каждый файл, найди маркеры:
# <<<<<<< HEAD
# твой код из main
# =======
# код из feature ветки
# >>>>>>> feature/название

# 3. Выбери нужный вариант (или объедини оба)
# Удали маркеры <<<<<<, ======, >>>>>>

# 4. Закоммить разрешение
git add .
git commit -m "merge: разрешены конфликты"
```

---

## 📚 Дополнительные материалы

- [Git Book (русский)](https://git-scm.com/book/ru/v2)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [GitHub Flow Guide](https://guides.github.com/introduction/flow/)

---

**Готово! Используй эту инструкцию для всех изменений в проекте.** 🚀

