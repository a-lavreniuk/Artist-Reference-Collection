# Стилизация карточек коллекций

## Дата: 9 ноября 2025
## Ветка: `feature/card-styling`

---

## ✅ Выполненные изменения

### 1. **Превью коллекции — сетка из 3 слотов**

**Соотношение сторон**: `16:9` (вместо квадрата)

**Структура:**
- **Первый слот** (левый): занимает всю высоту (2 строки)
- **Второй слот** (правый верхний): занимает половину высоты
- **Третий слот** (правый нижний): занимает половину высоты

**Оформление:**
- Скругление углов: `16px`
- Без обводки (border)
- **Без hover эффектов** (ни зума, ни смещения)

**Логика отображения:**
- Последние добавленные карточки отображаются **первыми**
- Превью формируется динамически из `thumbnailUrl` или `filePath`
- Если карточек меньше 3, пустые слоты заполняются **серыми плейсхолдерами**
- Если коллекция пуста, все 3 слота — серые плейсхолдеры

```css
.collection-card__grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: var(--spacing-2xs);
}

.collection-card__grid-item:first-child {
  grid-row: span 2; /* Первый элемент на 2 строки */
}

.collection-card__placeholder {
  background-color: var(--color-grayscale-200);
}
```

### 2. **Статистика по типам файлов**

**Иконки с количеством:**
- **Изображения**: иконка `image-16-border` + количество
- **Видео**: иконка `play-16-border` + количество

**Расположение:**
- **Внутри превью** в верхнем левом углу
- Отступ: `16px` от краёв
- `z-index: 2` — поверх изображений

**Стили (идентично Card.tsx):**
- Фон: `rgba(81, 79, 91, 0.48)`
- Размытие: `blur(64px)`
- Высота: `32px`
- Скругление: `var(--radius-s)`
- Цвет текста: `var(--text-light)`
- Отступ между иконками: `var(--spacing-xs)`

```tsx
<div className="collection-card__preview">
  {/* Сетка с превью */}
  <div className="collection-card__grid">...</div>
  
  {/* Статистика ВНУТРИ превью */}
  {totalCount > 0 && (
    <div className="collection-card__stats">
      {imageCount > 0 && (
        <div className="collection-card__stat">
          <Icon name="image" size={16} variant="border" />
          <span>{imageCount}</span>
        </div>
      )}
      {videoCount > 0 && (
        <div className="collection-card__stat">
          <Icon name="play" size={16} variant="border" />
          <span>{videoCount}</span>
        </div>
      )}
    </div>
  )}
</div>
```

### 3. **Упрощённая структура**

**Что убрано:**
- ❌ Обводка карточки (`border`)
- ❌ Описание коллекции (`description`)
- ❌ Общий счётчик карточек в правом верхнем углу
- ❌ Иконка папки в пустом состоянии
- ❌ Hover эффекты на всю карточку

**Что осталось:**
- ✅ Превью из 3 карточек (16:9)
- ✅ Статистика внутри превью (верхний левый угол)
- ✅ Название просто под превью (H4 типографика)
- ✅ Без hover эффектов

---

## 📦 Измененные файлы

1. `renderer/src/components/collections/CollectionCard.tsx`
2. `renderer/src/components/collections/CollectionCard.css`
3. `renderer/src/pages/CollectionsPage.tsx`

---

## 🔧 Новый API компонента CollectionCard

```typescript
export interface CollectionCardProps {
  collection: Collection;
  imageCount?: number;  // ← НОВЫЙ PROP: количество изображений
  videoCount?: number;  // ← НОВЫЙ PROP: количество видео
  onClick?: (collection: Collection) => void;
}
```

### Использование:

```tsx
<CollectionCard
  collection={collection}
  imageCount={15}  // Количество изображений в коллекции
  videoCount={3}   // Количество видео в коллекции
  onClick={handleClick}
/>
```

---

## 📝 Заметки для разработчиков

1. **Подсчёт статистики** выполняется в родительском компоненте (`CollectionsPage`)
2. **Превью** отображает последние 3 добавленные карточки (в обратном порядке)
3. **Плейсхолдеры** заполняют пустые слоты автоматически
4. **Статистика** показывается только если в коллекции есть карточки
5. **Сетка** адаптируется под любое количество превью (от 0 до 3)

---

## 🎨 Структура сетки превью

```
┌─────────┬─────────┐
│         │    2    │
│    1    ├─────────┤
│         │    3    │
└─────────┴─────────┘
```

**Слоты:**
1. Левый (большой) — последняя добавленная карточка
2. Правый верхний — предпоследняя карточка
3. Правый нижний — третья с конца карточка

---

## 🎯 Логика работы

### CollectionsPage загружает данные:
```typescript
const [collections, setCollections] = useState<Collection[]>([]);
const [cards, setCards] = useState<Card[]>([]);

// Загружаем коллекции и карточки параллельно
const [allCollections, allCards] = await Promise.all([
  getAllCollections(),
  getAllCards()
]);
```

### Подсчёт статистики:
```typescript
const getCollectionStats = (collection: Collection) => {
  const collectionCards = cards.filter(card => 
    collection.cardIds.includes(card.id)
  );
  const imageCount = collectionCards.filter(card => 
    card.type === 'image'
  ).length;
  const videoCount = collectionCards.filter(card => 
    card.type === 'video'
  ).length;
  return { imageCount, videoCount };
};
```

### Формирование превью (динамически):
```typescript
const getCollectionThumbnails = (collection: Collection): string[] => {
  // Получаем карточки коллекции в порядке добавления (последние первыми)
  const collectionCards = collection.cardIds
    .map(id => cards.find(card => card.id === id))
    .filter((card): card is Card => card !== undefined)
    .reverse(); // Последние добавленные первыми
  
  // Берём первые 3 и возвращаем их thumbnails или filePath
  return collectionCards
    .slice(0, 3)
    .map(card => card.thumbnailUrl || card.filePath);
};
```

---

## 📐 Размеры и отступы

- **Превью**: `aspect-ratio: 16 / 9` (широкоформатное)
- **Радиус превью**: `16px`
- **Gap между слотами**: `var(--spacing-2xs)`
- **Отступ статистики**: `16px` (от краёв превью)
- **Gap между иконками**: `var(--spacing-xs)`
- **Gap между превью и названием**: `var(--spacing-m)`
- **Высота метки статистики**: `32px`
- **Название (H4)**: `font-size-l`, `font-weight-bold`, `line-height-l`

---

## 🔄 Сравнение: До и После

### До:
- Сетка 2×2 (4 превью)
- Общий счётчик карточек
- Описание коллекции
- Иконка папки в пустом состоянии

### После:
- Сетка 1+2 (3 превью)
- Статистика по типам (изображения + видео)
- Только название
- Серые плейсхолдеры в пустом состоянии

---

## ✨ Особенности реализации

1. **Последняя добавленная карточка отображается первой** — достигается через `collection.thumbnails.slice(0, 3)`
2. **Автозаполнение плейсхолдерами** — всегда рендерим 3 слота, заполняя пустые серыми блоками
3. **Условная статистика** — иконки показываются только если есть файлы данного типа
4. **Параллельная загрузка данных** — `Promise.all` для коллекций и карточек


