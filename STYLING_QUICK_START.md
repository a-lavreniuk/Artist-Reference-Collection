# 🚀 Быстрый старт стилизации

## ✅ Текущий статус: ГОТОВ К СТИЛИЗАЦИИ (95%)

### Что уже сделано
- ✅ TypeScript компилируется без ошибок
- ✅ Все компоненты созданы (TSX + CSS)
- ✅ Шрифты подключены (YS Music, Geologica)
- ✅ CSS переменные определены (цвета, отступы, радиусы)
- ✅ База данных работает
- ✅ IPC handlers реализованы

### Что нужно добавить
- ⚠️ Иконка приложения (icon.ico) — 5 минут
- ⚠️ Тестовые данные для проверки UI

---

## 🎯 План действий

### 1. Запустить приложение
```bash
start-dev.bat
```

### 2. Добавить тестовые изображения
1. Откроется OnboardingScreen
2. Выбрать рабочую папку
3. Перейти на AddPage (кнопка "+" в Sidebar)
4. Добавить 5-10 изображений

### 3. Начать стилизацию

**Рекомендуемый порядок:**
1. **Sidebar** (уже есть стили) → проверить
2. **SearchBar + SearchDropdown** → доработать выпадающий список
3. **SectionHeader** → кнопки фильтров
4. **MasonryGrid** → сетка карточек
5. **Card** → стили карточки референса
6. **CardViewModal** → модальное окно

---

## 📁 Где находятся стили

```
renderer/src/
├── styles/
│   ├── variables.css    → CSS переменные (цвета, отступы)
│   ├── global.css        → Глобальные стили
│   └── typography.css    → Шрифты (YS Music, Geologica)
│
├── components/
│   ├── common/
│   │   ├── Button.css
│   │   ├── Input.css
│   │   ├── Modal.css
│   │   └── Card.css
│   │
│   ├── layout/
│   │   ├── Layout.css
│   │   ├── Sidebar.css
│   │   ├── SearchBar.css
│   │   └── SectionHeader.css
│   │
│   └── gallery/
│       ├── MasonryGrid.css
│       ├── CardViewModal.css
│       └── AddCardFlow.css
```

---

## 🎨 CSS переменные

### Цвета
```css
/* Текст */
--text-primary: #3B3946 (grayscale-900)
--text-secondary: #93919A (grayscale-500)
--text-light: #F5F4F7 (grayscale-50)

/* Фон */
--bg-primary: #F5F4F7 (grayscale-50)
--bg-secondary: #FFFFFF (grayscale-25)

/* Кнопки */
--bg-button-primary: #514F5B (grayscale-800)
--bg-button-secondary: #EBE9EE (grayscale-100)

/* Обводки */
--border-default: #EBE9EE (grayscale-100)
```

### Отступы
```css
--spacing-xs: 4px
--spacing-s: 8px
--spacing-m: 12px
--spacing-l: 16px
--spacing-xl: 24px
--spacing-2xl: 32px
```

### Скругления
```css
--radius-xs: 4px
--radius-s: 8px
--radius-m: 12px
--radius-l: 16px
```

---

## 🔧 Полезные команды

```bash
# Запустить dev режим
start-dev.bat

# DevTools в Electron
Ctrl + Shift + I

# Пересобрать renderer (если что-то сломалось)
cd renderer
npm run build
cd ..
```

---

## ⚙️ Настройки окна

- **Минимум:** 1920×1080
- **Максимум:** 2560×1440
- **Sidebar:** 72px ширина
- **Header:** 64px высота
- **Галерея:** 6 колонок (standard) или 10 колонок (compact)

---

## 📝 Быстрые советы

### Где смотреть примеры
- Дизайн в Figma (если есть ссылка)
- Существующие компоненты с готовыми стилями:
  - `Button.css` — полностью стилизован
  - `Sidebar.css` — готов к использованию
  - `Layout.css` — базовая структура

### Как проверить изменения
1. Сохранить CSS файл
2. Страница обновится автоматически (hot reload)
3. Проверить в приложении

### Если что-то не работает
1. Открыть DevTools (Ctrl+Shift+I)
2. Проверить Console на ошибки
3. Проверить Elements → Computed для CSS
4. Проверить Network на загрузку шрифтов

---

## 🐛 Возможные проблемы

### Шрифты не загружаются
**Решение:** Проверить пути в `typography.css`:
```css
url('/fonts/YSMusic-HeadlineBold.ttf')
```

### Стили не применяются
**Решение:** 
1. Проверить импорт в компоненте
2. Проверить специфичность селекторов
3. Использовать `!important` (только если необходимо)

### Приложение не запускается
**Решение:**
```bash
# Остановить процессы
Ctrl + C

# Очистить и пересобрать
cd renderer
npm run build
cd ..
start-dev.bat
```

---

**Готово! Можно начинать стилизацию! 🎨**



