# Отчет по анализу проекта ARC
## Artist Reference Collection - Анализ багов, несостыковок и слабых мест

**Дата анализа:** 3 декабря 2025  
**Версия:** 1.2.8  
**Анализировал:** AI Code Analyzer

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Отсутствует критически важный файл `download-manager.ts`

**Серьезность:** КРИТИЧЕСКАЯ  
**Файлы:** `src/main/main.ts:12`, `src/main/ipc-handlers.ts:16`

**Проблема:**
Файл `src/main/download-manager.ts` импортируется в двух местах, но физически отсутствует в проекте.

```typescript
// src/main/main.ts:12
import { DownloadManager } from './download-manager';

// src/main/ipc-handlers.ts:16
import { DownloadManager } from './download-manager';
```

**Места использования:**
- `main.ts:60` - `DownloadManager.getInstance().setWorkingDirectory(selectedPath)`
- `main.ts:76` - `DownloadManager.getInstance().setWorkingDirectory(dirPath)`
- `main.ts:219` - `DownloadManager.getInstance().setMainWindow(mainWindow)`
- `main.ts:486` - `DownloadManager.getInstance().cleanupOldFiles()`
- `ipc-handlers.ts:122` - использование в обработке протокола arc://

**Последствия:**
- ❌ Приложение **НЕ ЗАПУСТИТСЯ** - ошибка при импорте модуля
- ❌ TypeScript не скомпилируется
- ❌ Невозможно собрать production build
- ❌ Функциональность импорта из браузера не работает

**Решение:**
Необходимо создать файл `src/main/download-manager.ts` с реализацией класса DownloadManager, который должен содержать:
- Singleton pattern (`getInstance()`)
- Методы: `setWorkingDirectory()`, `setMainWindow()`, `cleanupOldFiles()`, `downloadFile()`, `getTempDir()`

---

### 2. Несоответствие между интерфейсом ElectronAPI и реализацией в preload

**Серьезность:** ВЫСОКАЯ  
**Файлы:** `renderer/src/types/electron.d.ts`, `src/preload/preload.ts`

**Проблема:**
В `electron.d.ts` объявлены методы, которых нет в реализации `preload.ts`, и наоборот.

**Методы объявлены в типах, но ОТСУТСТВУЮТ в preload.ts:**

```typescript
// Нигде не реализованы:
setWorkingDirectory: (dirPath: string) => Promise<boolean>;
scanImportDirectory: () => Promise<string[]>;
moveFileToWorkingDir: (sourcePath: string, workingDir: string) => Promise<string>;
```

**Методы реализованы в preload.ts, но ОТСУТСТВУЮТ в типах:**

```typescript
// Строки 346-360 в preload.ts:
setWorkingDirectory: (dirPath: string) => ipcRenderer.invoke('set-working-directory', dirPath),
scanImportDirectory: () => ipcRenderer.invoke('scan-import-directory'),
moveFileToWorkingDir: (sourcePath: string, workingDir: string) => 
  ipcRenderer.invoke('move-file-to-working-dir', sourcePath, workingDir),
```

**Последствия:**
- ⚠️ TypeScript не видит ошибки типов при использовании этих методов
- ⚠️ Потенциальные runtime ошибки
- ⚠️ Нарушается type safety
- ⚠️ IDE не предоставляет автодополнение

**Решение:**
Синхронизировать типы в `electron.d.ts` с реализацией в `preload.ts`.

---

### 3. Хардкод Windows-путей вместо кроссплатформенного path.join

**Серьезность:** ВЫСОКАЯ (для кроссплатформенности)  
**Файлы:** `renderer/src/services/db.ts`

**Проблема:**
В коде используются жестко зашитые Windows-разделители пути `\\` вместо `path.join` или `path.sep`.

```typescript
// db.ts:291
const thumbPath = workingDir.replace(/[\/\\]$/, '') + '\\_cache\\thumbs\\' + thumbName;

// db.ts:888
card.filePath = newWorkingDir + '\\' + match[1].replace(/\//g, '\\');

// db.ts:899
card.thumbnailUrl = `${newWorkingDir}\\${thumbRelativePath}`;
```

**Последствия:**
- ❌ Приложение НЕ БУДЕТ РАБОТАТЬ на macOS и Linux
- ❌ Нарушение заявленной кроссплатформенности Electron
- ❌ Невозможность тестирования на других ОС

**Решение:**
Заменить все хардкод-пути на использование Node.js `path` модуля:
```typescript
const thumbPath = path.join(workingDir, '_cache', 'thumbs', thumbName);
card.filePath = path.join(newWorkingDir, match[1]);
```

---

## ⚠️ ВАЖНЫЕ ПРОБЛЕМЫ

### 4. Архитектурная проблема: использование globalShortcut вместо локальных hotkeys

**Серьезность:** СРЕДНЯЯ  
**Файл:** `src/main/shortcuts.ts`

**Проблема:**
Используются глобальные горячие клавиши (`globalShortcut`), которые работают даже когда приложение не в фокусе.

```typescript
// shortcuts.ts:18
globalShortcut.register('CommandOrControl+N', () => {
  mainWindow?.webContents.send('hotkey:new-card');
});

// shortcuts.ts:29
globalShortcut.register('Delete', () => {
  mainWindow?.webContents.send('hotkey:delete');
});
```

**Проблемы:**
- ⚠️ Клавиша `Delete` - это СИСТЕМНАЯ клавиша, нельзя перехватывать глобально
- ⚠️ `Ctrl+N`, `Ctrl+F` могут конфликтовать с другими приложениями
- ⚠️ Пользователь не сможет использовать эти комбинации в других программах
- ⚠️ Плохая практика UX

**Рекомендация:**
Использовать локальные accelerators через `webContents.on('before-input-event')` или React обработчики событий.

---

### 5. Отсутствует обработка ошибок в check-for-updates

**Серьезность:** СРЕДНЯЯ  
**Файлы:** `src/main/ipc-handlers.ts:971-974`, `src/main/ipc-handlers.ts:980-983`

**Проблема:**
IPC handlers для обновлений не реализованы (TODO), но вызываются из UI.

```typescript
// ipc-handlers.ts:971
ipcMain.handle('check-for-updates', async () => {
  console.log('[IPC] Проверка обновлений...');
  // TODO: Реализовать проверку обновлений
});

// ipc-handlers.ts:980
ipcMain.handle('install-update', async () => {
  console.log('[IPC] Установка обновления...');
  // TODO: Реализовать установку обновления
});
```

**Последствия:**
- ⚠️ Кнопки обновления в UI не работают
- ⚠️ Функционал auto-updater не интегрирован с UI
- ⚠️ Пользователи не могут обновиться из приложения

**Решение:**
Интегрировать функции из `auto-updater.ts` в IPC handlers:
```typescript
ipcMain.handle('check-for-updates', async () => {
  await checkForUpdates();
});
```

---

### 6. Потенциальная проблема с безопасностью: file:// URLs

**Серьезность:** СРЕДНЯЯ  
**Файл:** `src/main/ipc-handlers.ts:511`

**Проблема:**
Прямое создание file:// URL без валидации пути.

```typescript
// ipc-handlers.ts:511
return `file:///${filePath.replace(/\\/g, '/')}`;
```

**Уязвимости:**
- ⚠️ Path traversal атаки (`../../../`)
- ⚠️ Доступ к файлам вне рабочей директории
- ⚠️ Потенциальная утечка системных файлов

**Рекомендация:**
Добавить валидацию, что файл находится внутри рабочей директории:
```typescript
const workingDir = await getSetting('workingDirectory');
const normalizedPath = path.normalize(filePath);
if (!normalizedPath.startsWith(workingDir)) {
  throw new Error('Access denied: file outside working directory');
}
```

---

### 7. Неэффективная виртуализация в MasonryGrid

**Серьезность:** СРЕДНЯЯ  
**Файл:** `renderer/src/components/gallery/MasonryGrid.tsx`

**Проблема:**
Используется двойная виртуализация: встроенная в `react-masonry-css` и кастомная через IntersectionObserver.

```typescript
// MasonryGrid.tsx:52-101
const [visibleCards, setVisibleCards] = useState<Set<string>>(new Set());
// ... IntersectionObserver logic

// MasonryGrid.tsx:165-168
<Masonry
  breakpointCols={breakpointColumns}
  className="masonry-grid__container"
```

**Проблемы:**
- ⚠️ Двойная работа: `react-masonry-css` сам рендерит все элементы, а IntersectionObserver управляет видимостью
- ⚠️ Все карточки создаются в DOM, но скрываются (не истинная виртуализация)
- ⚠️ При 10,000+ карточек будут проблемы с производительностью
- ⚠️ `react-masonry-css` не поддерживает истинную виртуализацию

**Рекомендация:**
Использовать `react-window` или `react-virtuoso` с кастомным masonry layout для истинной виртуализации.

---

### 8. Отсутствует лимит на размер логов

**Серьезность:** СРЕДНЯЯ  
**Файл:** `src/main/main.ts:14-34`

**Проблема:**
Логи пишутся в файл неограниченно через `fs.createWriteStream` с флагом `'a'` (append).

```typescript
// main.ts:16
const logStream = fs.createWriteStream(logFile, { flags: 'a' });
```

**Последствия:**
- ⚠️ Файл логов будет расти бесконечно
- ⚠️ Может занять гигабайты дискового пространства
- ⚠️ Потенциальная причина жалоб пользователей

**Рекомендация:**
Использовать ротацию логов или лимит размера файла (например, winston или bunyan логгер).

---

## 📋 СРЕДНИЕ ПРОБЛЕМЫ

### 9. Неоптимальная работа с превью в db.ts

**Серьезность:** НИЗКАЯ-СРЕДНЯЯ  
**Файл:** `renderer/src/services/db.ts:278-296`

**Проблема:**
При удалении карточки пытается удалить превью двумя способами одновременно.

```typescript
// db.ts:278-296
if (card.thumbnailUrl && !card.thumbnailUrl.startsWith('data:')) {
  await window.electronAPI.deleteFile(card.thumbnailUrl);
} else {
  // Также пытается удалить превью по предполагаемому пути
  const fileName = card.filePath.split(/[\\/]/).pop()?.replace(/\.[^.]+$/, '');
  if (fileName) {
    const thumbName = `${fileName}_thumb.jpg`;
    const thumbPath = workingDir.replace(/[\/\\]$/, '') + '\\_cache\\thumbs\\' + thumbName;
    await window.electronAPI.deleteFile(thumbPath);
  }
}
```

**Проблемы:**
- ⚠️ Дублирование попыток удаления
- ⚠️ Не гарантирует что файл превью будет удален
- ⚠️ Лишние обращения к файловой системе

**Рекомендация:**
Упростить логику и всегда сохранять путь к превью в `thumbnailUrl` (не Data URL).

---

### 10. Отсутствует проверка формата файлов при импорте

**Серьезность:** СРЕДНЯЯ  
**Файл:** `src/main/ipc-handlers.ts:147`

**Проблема:**
Поддерживаемые форматы жестко заданы, но нет валидации MIME-типов.

```typescript
// ipc-handlers.ts:147
const supportedExtensions = ['.jpg', '.jpeg', '.png', '.webp', '.mp4', '.webm'];
```

**Проблемы:**
- ⚠️ Файл `.jpg` может оказаться переименованным `.png`
- ⚠️ Поврежденные файлы не отфильтровываются
- ⚠️ Потенциальные проблемы с генерацией превью

**Рекомендация:**
Проверять magic bytes файлов или использовать библиотеку типа `file-type`.

---

### 11. Некорректная обработка больших файлов в get-file-url

**Серьезность:** СРЕДНЯЯ  
**Файл:** `src/main/ipc-handlers.ts:499-537`

**Проблема:**
Для файлов >5MB возвращается file:// URL, но в Electron это может не работать из-за CSP.

```typescript
// ipc-handlers.ts:506-511
if (fileSizeInMB > maxSizeForDataURL) {
  console.log(`[IPC] Файл слишком большой (${fileSizeInMB.toFixed(2)}MB), используем file:// URL`);
  return `file:///${filePath.replace(/\\/g, '/')}`;
}
```

**Проблемы:**
- ⚠️ file:// URLs могут не работать с webSecurity включенным
- ⚠️ Renderer процесс может не иметь доступа к локальным файлам
- ⚠️ CSP может блокировать загрузку

**Рекомендация:**
Использовать custom protocol (например, `app://`) или стримить файлы через IPC.

---

### 12. Неоптимальный алгоритм разбиения архивов

**Серьезность:** НИЗКАЯ-СРЕДНЯЯ  
**Файл:** `src/main/ipc-handlers.ts:1448-1478`

**Проблема:**
Весь файл загружается в память перед разбиением на части.

```typescript
// ipc-handlers.ts:1452
const fileBuffer = await fs.readFile(filePath);
const fileSize = fileBuffer.length;
```

**Проблемы:**
- ⚠️ Для архива в 8GB требуется 8GB RAM
- ⚠️ Может вызвать out-of-memory на слабых системах
- ⚠️ Медленная работа на больших файлах

**Рекомендация:**
Использовать потоковое чтение/запись (streams) вместо загрузки в память.

---

### 13. История действий может стать огромной

**Серьезность:** НИЗКАЯ-СРЕДНЯЯ  
**Файл:** `src/main/ipc-handlers.ts:1264-1322`

**Проблема:**
История ограничена 1000 записями, но хранится весь JSON в памяти и перезаписывается каждый раз.

```typescript
// ipc-handlers.ts:1309
if (history.length > 1000) {
  history = history.slice(0, 1000);
}
```

**Проблемы:**
- ⚠️ При каждом добавлении записи читается и пишется весь файл
- ⚠️ 1000 записей с метаданными могут занимать много места
- ⚠️ Неэффективно для частых операций

**Рекомендация:**
Использовать circular buffer или базу данных (SQLite).

---

## 🟡 НЕСОСТЫКОВКИ И УЛУЧШЕНИЯ

### 14. Дублирование интерфейса ElectronAPI

**Файлы:** `src/preload/preload.ts:11-339`, `renderer/src/types/electron.d.ts:5-109`

**Проблема:**
Интерфейс определен дважды: в preload.ts и в electron.d.ts. При этом они не синхронизированы.

**Рекомендация:**
Вынести интерфейс в отдельный файл `src/types/electron-api.ts` и импортировать в обоих местах.

---

### 14. Смешение ответственности в db.ts

**Файл:** `renderer/src/services/db.ts`

**Проблема:**
Файл db.ts содержит 1093 строки кода и выполняет слишком много задач:
- Определение схемы БД (строки 1-123)
- CRUD операции (строки 168-563)
- Бизнес-логика (строки 565-632)
- Поиск (строки 726-798)
- Экспорт/импорт (строки 836-923)
- Статистика (строки 929-998)
- Утилиты (строки 1005-1089)

**Рекомендация:**
Разбить на модули:
- `db/schema.ts` - схема
- `db/cards.ts` - операции с карточками
- `db/tags.ts` - операции с метками
- `db/collections.ts` - операции с коллекциями
- `db/moodboard.ts` - операции с мудбордом
- `db/search.ts` - поисковые функции
- `db/export.ts` - импорт/экспорт

---

### 15. Отсутствует валидация данных перед сохранением

**Файлы:** `renderer/src/services/db.ts`

**Проблема:**
Нет проверок:
- Пустые строки в названиях
- Дублирование имен коллекций/категорий
- Валидность путей к файлам
- Максимальная длина строк

**Рекомендация:**
Добавить схему валидации (например, Zod или Yup) для всех типов данных.

---

### 16. Нет обработки race conditions в мудборде

**Файл:** `renderer/src/services/db.ts:587-622`

**Проблема:**
При быстром добавлении/удалении карточек из мудборда возможны конфликты.

```typescript
// db.ts:590-591
const moodboard = await getMoodboard();
// ... другие операции
await db.moodboard.update('default', { ... });
```

**Проблемы:**
- ⚠️ Если два вызова выполняются параллельно, один перезапишет изменения другого
- ⚠️ Потеря данных при конкурентном доступе

**Рекомендация:**
Использовать транзакции Dexie или optimistic locking.

---

### 17. Использование console.log вместо proper logging

**Файлы:** Все файлы проекта

**Проблема:**
Везде используется `console.log` вместо структурированного логирования.

**Рекомендации:**
- Создать логгер-обертку с уровнями (debug, info, warn, error)
- Добавить префиксы контекста
- Возможность отключения debug-логов в production

---

### 18. Отсутствуют ESLint правила для безопасности

**Файл:** Нет `.eslintrc` в корне проекта

**Проблема:**
Нет автоматических проверок на:
- XSS уязвимости
- SQL инъекции в IndexedDB запросы
- Path traversal
- Небезопасное использование eval, innerHTML

**Рекомендация:**
Добавить ESLint с плагинами:
- `eslint-plugin-security`
- `eslint-plugin-react-hooks`
- `@typescript-eslint`

---

### 19. Нет тестов

**Проблема:**
В проекте полностью отсутствуют:
- Unit тесты
- Integration тесты
- E2E тесты

**Рекомендация:**
Добавить тестирование:
- Jest для unit-тестов
- React Testing Library для компонентов
- Playwright для E2E тестов Electron

---

### 20. Проблемы с обновлениями в electron-builder.yml

**Файл:** `electron-builder.yml:78-83`

**Проблема:**
Настройка auto-update указывает на неправильный репозиторий.

```yaml
publish:
  provider: github
  owner: rocontd
  repo: arc  # Должно быть: Artist-Reference-Collection
```

**Последствия:**
- ⚠️ Автообновления не будут работать
- ⚠️ Пользователи не получат обновления

**Решение:**
Исправить `repo: arc` на `repo: Artist-Reference-Collection`.

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ЗАМЕЧАНИЯ

### 21. Удаленные документы

В git status видны удаленные MD файлы:
- `GITHUB_RELEASE_*.md`
- `PROJECT_STATUS.md`
- `TECHNICAL_DOCUMENTATION.md`
- `docs/*.md`

**Проблема:**
Документация удалена, но не закоммичена. Возможно, это было сделано случайно.

**Рекомендация:**
Восстановить критическую документацию или закоммитить удаление.

---

### 22. Отсутствует обработка ошибок в useFileSystem

**Файл:** `renderer/src/hooks/useFileSystem.ts`

**Проблема:**
Ошибки записываются в state, но не показываются пользователю.

```typescript
// useFileSystem.ts:87
setError(err as Error);
```

**Рекомендация:**
Добавить toast уведомления или error boundary для показа ошибок пользователю.

---

## 📊 СТАТИСТИКА ПРОБЛЕМ

| Серьезность | Количество |
|-------------|------------|
| 🔴 Критические | 3 |
| ⚠️ Важные | 5 |
| 📋 Средние | 5 |
| 🟡 Улучшения | 9 |
| **ВСЕГО** | **22** |

---

## ✅ РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### 🔥 СРОЧНО (блокируют запуск):

1. **Создать файл `download-manager.ts`** - без него приложение не запустится
2. **Синхронизировать типы ElectronAPI** - исправить несоответствия между interface и implementation
3. **Исправить electron-builder.yml** - обновления не работают

### ⚡ ВАЖНО (влияют на пользователей):

4. **Заменить globalShortcut на локальные hotkeys** - конфликты с системой
5. **Реализовать обработчики check-for-updates** - не работают кнопки обновления
6. **Добавить валидацию путей** - уязвимость безопасности
7. **Исправить кроссплатформенность путей** - не работает на Mac/Linux

### 🔧 УЛУЧШЕНИЯ (технический долг):

8. **Рефакторинг db.ts** - разбить на модули
9. **Добавить тесты** - минимум критичные пути
10. **Заменить виртуализацию в MasonryGrid** - производительность
11. **Добавить ротацию логов** - не засорять диск

---

## 🎯 ЗАКЛЮЧЕНИЕ

Проект **ARC** имеет солидную архитектуру и хорошо структурирован, но содержит **критические проблемы**, которые блокируют его запуск и использование:

✅ **Сильные стороны:**
- Четкое разделение main/renderer процессов
- Использование TypeScript для type safety
- Продуманная схема базы данных IndexedDB
- Хорошая организация компонентов

❌ **Критические недостатки:**
- Отсутствующий файл `download-manager.ts` делает приложение нерабочим
- Несоответствие типов и реализации может привести к runtime ошибкам
- Хардкод Windows-путей ломает кроссплатформенность
- Отсутствие тестов делает рефакторинг опасным

**Рекомендуемый порядок исправления:** следовать списку приоритетов выше, начиная с критических проблем.

---

**Конец отчета**

