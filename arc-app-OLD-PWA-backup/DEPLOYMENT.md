# Руководство по развёртыванию ARC PWA

## 🚀 Production Build

### Создание production сборки

```bash
cd arc-app
npm run build
```

Это создаст оптимизированную версию в папке `dist/`.

### Что происходит при сборке:

1. ✅ TypeScript компилируется в JavaScript
2. ✅ CSS оптимизируется и минифицируется
3. ✅ JavaScript бандлится и минифицируется
4. ✅ Изображения и шрифты копируются
5. ✅ Service Worker генерируется автоматически
6. ✅ PWA Manifest включается

---

## 📦 Варианты развёртывания

### Вариант 1: GitHub Pages (Рекомендуется)

Бесплатный хостинг для статичных сайтов.

**Шаги:**

1. **Обнови `vite.config.ts`:**

```typescript
export default defineConfig({
  base: '/arc/',  // Название репозитория
  // ... остальные настройки
})
```

2. **Создай сборку:**

```bash
npm run build
```

3. **Установи gh-pages:**

```bash
npm install --save-dev gh-pages
```

4. **Добавь скрипт в `package.json`:**

```json
"scripts": {
  "deploy": "gh-pages -d dist"
}
```

5. **Деплой:**

```bash
npm run deploy
```

6. **Настрой GitHub Pages:**
   - Зайди в Settings → Pages
   - Source: Deploy from branch
   - Branch: gh-pages
   - Готово! Приложение будет доступно по адресу:
     `https://a-lavreniuk.github.io/arc/`

---

### Вариант 2: Локальный сервер

Для личного использования на одном компьютере.

**Шаги:**

1. **Создай сборку:**

```bash
npm run build
```

2. **Установи http-server глобально:**

```bash
npm install -g http-server
```

3. **Запусти сервер:**

```bash
cd dist
http-server -p 8080
```

4. **Открой в браузере:**
   `http://localhost:8080`

5. **Автозапуск с Windows (опционально):**

Создай файл `start-arc.bat`:

```batch
@echo off
cd /d C:\GitHub\arc\arc-app\dist
http-server -p 8080
```

Помести его в Автозагрузку:
`C:\Users\%USERNAME%\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup`

---

### Вариант 3: Vercel (Бесплатно)

Современный хостинг с автоматическим деплоем.

**Шаги:**

1. **Установи Vercel CLI:**

```bash
npm install -g vercel
```

2. **Деплой:**

```bash
cd arc-app
vercel
```

3. **Следуй инструкциям в терминале**

4. **Production деплой:**

```bash
vercel --prod
```

Приложение будет доступно по адресу типа `arc-app.vercel.app`

---

### Вариант 4: Netlify (Бесплатно)

Ещё один отличный хостинг для PWA.

**Через веб-интерфейс:**

1. Зайди на [netlify.com](https://netlify.com)
2. Drag & drop папку `dist/` на сайт
3. Готово!

**Через CLI:**

```bash
npm install -g netlify-cli
cd arc-app
netlify deploy --prod
```

---

## 🔧 Настройка для production

### Обновление версии

В `package.json`:

```json
{
  "version": "0.1.0"
}
```

### Оптимизация Service Worker

В `vite.config.ts` уже настроено:

```typescript
workbox: {
  globPatterns: ['**/*.{js,css,html,ico,png,svg,woff,woff2}'],
  runtimeCaching: [/* ... */]
}
```

---

## 📱 Установка PWA

После развёртывания пользователи могут установить ARC:

### На Desktop:

1. Открой приложение в Chrome/Edge
2. В адресной строке появится иконка установки ⊕
3. Клик → "Установить"
4. PWA установится как обычная программа

### Через меню браузера:

- Chrome: ⋮ → "Установить ARC..."
- Edge: ⋯ → "Приложения" → "Установить это приложение"

---

## 🧪 Тестирование production build

```bash
# Создай сборку
npm run build

# Просмотр локально
npm run preview
```

Откроется на `http://localhost:4173`

---

## ✅ Чеклист перед деплоем

- [ ] Проверь что все изображения и ресурсы загружаются
- [ ] Протестируй Service Worker (DevTools → Application → Service Workers)
- [ ] Проверь PWA Manifest (DevTools → Application → Manifest)
- [ ] Протестируй офлайн режим (DevTools → Network → Offline)
- [ ] Проверь установку PWA
- [ ] Протестируй на разных разрешениях (1920×1080 и 2560×1440)
- [ ] Проверь все разделы: Карточки, Коллекции, Метки, Мудборд, Настройки
- [ ] Создай тестовые данные и протестируй функционал

---

## 🔄 Обновления

При новых изменениях:

1. Увеличь версию в `package.json`
2. Обнови `CHANGELOG.md`
3. Создай новую сборку: `npm run build`
4. Деплой: `npm run deploy` (или команда выбранного хостинга)

Service Worker автоматически уведомит пользователей о доступном обновлении.

---

## 📊 Мониторинг

### Lighthouse

Проверь качество PWA:

1. Открой DevTools
2. Вкладка Lighthouse
3. Выбери "Progressive Web App"
4. Клик "Analyze"

Цель: **90+ баллов**

### Проверка в разных браузерах:

- ✅ Google Chrome
- ✅ Microsoft Edge  
- ✅ Яндекс Браузер
- ❌ Firefox (File System API не поддерживается)
- ❌ Safari (File System API не поддерживается)

---

**Готов к развёртыванию!** 🚀

