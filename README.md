# ARC — Artist Reference Collection

Персональное офлайн настольное приложение для организации визуальных референсов дизайнеров.

## О проекте

**ARC** — это полноценное desktop-приложение для Windows 10/11, созданное на базе Electron. Приложение предназначено для каталогизации и организации визуальных референсов — изображений и видео, которые используют дизайнеры, художники и другие творческие специалисты в своей работе.

### Ключевые особенности

- 🔒 **Полностью офлайн** — работает без интернета, все данные хранятся локально
- ⚡ **Высокая производительность** — поддержка коллекций до 50,000 файлов
- 🎨 **Современный UI** — адаптирован под разрешения 1920×1080 — 2560×1440
- 📁 **Умная организация** — система тегов, категорий и коллекций
- 🔍 **Быстрый поиск** — полнотекстовый поиск с саджестами
- 📊 **Мудборды** — создание визуальных коллажей из референсов
- 🔄 **Автообновления** — встроенная система обновлений через electron-updater

## Технологии

- **Electron 28+** — основа настольного приложения
- **React 18+** — UI фреймворк
- **TypeScript 5.3+** — язык разработки
- **IndexedDB (Dexie.js)** — база данных для метаданных
- **Node.js File System API** — работа с файловой системой
- **Vite** — сборщик для renderer процесса
- **Sharp** — обработка изображений
- **FFmpeg** — работа с видео

## Требования

- **Node.js** 18+ (рекомендуется 20.x)
- **npm** или yarn
- **Windows** 10/11

## Установка и запуск

### Установка зависимостей

```bash
# Установить зависимости главного процесса
npm install

# Установить зависимости renderer (React приложения)
cd renderer
npm install
cd ..
```

### Запуск в режиме разработки

```bash
# Быстрый запуск через bat-файл
start-dev.bat

# Или через npm
npm run dev
```

Это запустит:
1. Vite dev-сервер для React приложения (http://localhost:5173)
2. Electron с горячей перезагрузкой

### Сборка приложения

```bash
# Собрать TypeScript в JavaScript
npm run build

# Собрать установщик для Windows
npm run electron:build
```

Установщик будет создан в папке `release/`.

## Структура проекта

```
arc/
├── src/
│   ├── main/          # Исходный код Main Process (TypeScript)
│   └── preload/       # Исходный код Preload Script (TypeScript)
├── main/              # Скомпилированный Main Process (JavaScript)
├── preload/           # Скомпилированный Preload Script (JavaScript)
├── renderer/          # React приложение (Renderer Process)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── hooks/         # Кастомные хуки
│   │   ├── services/      # Сервисы (БД, файловая система)
│   │   ├── styles/        # Глобальные стили
│   │   └── types/         # TypeScript типы
│   ├── dist/          # Собранное приложение
│   └── package.json
├── resources/         # Иконки и ресурсы для сборки
├── scripts/          # Вспомогательные скрипты
├── docs/             # Документация проекта
├── package.json      # Зависимости Main Process
├── electron-builder.yml  # Конфигурация сборки
└── tsconfig.*.json   # TypeScript конфигурация

```

## Архитектура Electron

### Main Process (`src/main/main.ts`)
Управляет окном приложения, системными функциями, автообновлениями и обработкой IPC запросов.

### Preload Script (`src/preload/preload.ts`)
Безопасный мост между Main и Renderer процессами. Экспортирует API в `window.electronAPI`.

### Renderer Process (`renderer/`)
React UI приложение с полным функционалом для работы с карточками, тегами, коллекциями и мудбордами.

## API для Renderer

Доступен через `window.electronAPI`:

```typescript
// Выбор рабочей папки
const path = await window.electronAPI.selectWorkingDirectory();

// Сканирование директории
const files = await window.electronAPI.scanDirectory(path);

// Информация о файле
const info = await window.electronAPI.getFileInfo(filePath);

// Создание превью
const thumbPath = await window.electronAPI.generateThumbnail(filePath, workingDir);

// Импорт файлов
await window.electronAPI.importFiles(files, workingDir);
```

Полный список API см. в `src/preload/preload.ts`.

## База данных

Приложение использует **IndexedDB** через библиотеку **Dexie.js**:

- **cards** — карточки референсов
- **tags** — теги для классификации
- **categories** — категории тегов
- **collections** — пользовательские коллекции
- **moodboard_items** — элементы мудбордов
- **app_settings** — настройки приложения

## Файловая система

Структура хранения файлов:

```
{рабочая_папка}/
├── 2024/
│   ├── 01/
│   │   ├── 15/
│   │   │   ├── image1.jpg
│   │   │   └── video1.mp4
│   │   └── ...
│   └── ...
└── _cache/
    └── thumbs/
        ├── thumb1.jpg
        └── thumb2.jpg
```

## Горячие клавиши

- `Ctrl+N` — Создать новую карточку
- `Ctrl+F` — Открыть поиск
- `Delete` — Удалить выбранные карточки

## Документация

Подробная документация находится в папке `docs/`:

- [`docs/requirements.md`](docs/requirements.md) — Требования к проекту
- [`docs/technical.md`](docs/technical.md) — Техническая документация
- [`docs/development.md`](docs/development.md) — Руководство по разработке
- [`docs/user-guide.md`](docs/user-guide.md) — Руководство пользователя
- [`DEVELOPMENT_WORKFLOW.md`](DEVELOPMENT_WORKFLOW.md) — Процесс разработки
- [`TODO.md`](TODO.md) — Список задач

## Разработка

См. [`DEVELOPMENT_WORKFLOW.md`](DEVELOPMENT_WORKFLOW.md) для подробного описания процесса разработки.

## Лицензия

MIT © 2025 Александр Лавренюк
